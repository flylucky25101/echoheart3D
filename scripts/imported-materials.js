'use strict';
// Imported-asset hooks. Currently empty: the game uses its own procedural assets.
// (The importer tools/import_assets.py repopulates this when you import CC0 models.)
(function () {
  var EH = window.EchoHeart = window.EchoHeart || {};
  EH.__imp = {};
  EH.__impManifest = {};
  EH.__enemyMesh = {};
  EH.__weaponMesh = {};
  EH.__weaponImported = {};
  // no playerMesh override -> original 'lian'
})();
