# TEST_REPORT — ECHOHEART (3D 빌드 · 흰 박스 버그 수정본)

사실만 기록합니다. 검증하지 못한 항목은 8절에 명시했습니다.

## 1. 검수 방식

| 방식 | 수행 | 비고 |
|---|---|---|
| 정적 문법 검사 (`node --check`) | 수행 | 21개 스크립트 전부 통과 |
| 헤드리스 통합 테스트 (DOM+WebGL2 모킹, 실제 게임 코드 구동) | 수행 | 54개 검사 |
| 자체 테스트 스위트 (`tests.html` 로직) | 수행 | 54개 검사 |
| 스프라이트/메시/텍스처 실측 검증 | 수행 | 아래 참조 |
| 금지 패턴 정적 스캔 | 수행 | 전부 0건 |
| **실제 브라우저 `file://` 플레이** | **미수행** | 제작 환경에 브라우저 없음 |

## 2. 자동 테스트 결과

```
헤드리스 통합   총 56  통과 56  실패 0
자체 테스트     총 66  통과 66  실패 0
합계            총 122  통과 122  실패 0
```

### 헤드리스 통합 테스트

| # | 검사 | 결과 |
|---:|---|---|
| 1 | EchoHeart namespace built | PASS |
| 2 | all 21 scripts executed without throwing | PASS |
| 3 | renderer initialised (WebGL2) | PASS |
| 4 | sprite shader compiled + linked | PASS |
| 5 | all sprite sheets bound to GPU — `15 textures` | PASS |
| 6 | sprite animation table loaded — `93 clips` | PASS |
| 7 | game started (title) | PASS |
| 8 | hub is a 3D scene with devices | PASS |
| 9 | hub renders draw calls — `150 calls` | PASS |
| 10 | player moves in hub | PASS |
| 11 | run started, room 1 is combat | PASS |
| 12 | first room always combat | PASS |
| 13 | enemies spawned in room 1 — `2 enemies` | PASS |
| 14 | enemies have resolvable sprite frames | PASS |
| 15 | melee attack damages an enemy — `hp 34 -> 0` | PASS |
| 16 | hit feedback: particles spawned | PASS |
| 17 | ult charges from hitting | PASS |
| 18 | dash triggers and sets cooldown | PASS |
| 19 | skill triggers and sets cooldown | PASS |
| 20 | bow fires 3D projectiles | PASS |
| 21 | room cleared -> echo reward screen — `screen=reward` | PASS |
| 22 | reward offers 3 distinct echoes | PASS |
| 23 | echo applied to live stats | PASS |
| 24 | next-room choice screen shown | PASS |
| 25 | reached the boss room — `roomIndex=6` | PASS |
| 26 | boss spawned with sprite frames | PASS |
| 27 | boss intro completed | PASS |
| 28 | boss transitions through 3 phases — `phases=0,1,2` | PASS |
| 29 | boss attacks were telegraphed | PASS |
| 30 | life core consumed on lethal hit | PASS |
| 31 | revive restores 50% max HP — `hp=50.0` | PASS |
| 32 | revive grants i-frames | PASS |
| 33 | i-frames block damage | PASS |
| 34 | boss death fires exactly once | PASS |
| 35 | victory screen shown — `screen=victory` | PASS |
| 36 | win recorded once | PASS |
| 37 | crystals awarded on victory | PASS |
| 38 | abyss unlocked after first win | PASS |
| 39 | abyss tier raised | PASS |
| 40 | player death triggers once | PASS |
| 41 | game over screen shown — `screen=gameOver` | PASS |
| 42 | death counted once | PASS |
| 43 | projectile adaptation gained | PASS |
| 44 | crystals granted even on death | PASS |
| 45 | meta upgrade raises max HP — `100 -> 112` | PASS |
| 46 | next run uses upgraded stats — `112 vs 112` | PASS |
| 47 | adaptation reduces projectile damage next run — `100 -> 96.0` | PASS |
| 48 | save export/import round trip | PASS |
| 49 | corrupt save handled gracefully | PASS |
| 50 | pause screen opens | PASS |
| 51 | simulation frozen while paused | PASS |
| 52 | frames rendered without error — `28789 draw calls` | PASS |
| 53 | camera produces finite screen coords | PASS |
| 54 | player sprite resolves in all 8 facings | PASS |

### 자체 테스트 스위트

| # | 검사 | 결과 |
|---:|---|---|
| 1 | WebGL2 컨텍스트 생성 | PASS |
| 2 | 스프라이트 셰이더 컴파일/링크 | PASS |
| 3 | 스프라이트 시트 존재 — `14개` | PASS |
| 4 | 애니메이션 정의 존재 — `93개` | PASS |
| 5 | 시트 경로가 모두 상대경로 | PASS |
| 6 | 모든 클립의 셀 범위가 시트 안에 있음 | PASS |
| 7 | 모든 클립에 앵커(발 위치) 존재 | PASS |
| 8 | 모든 클립에 baked 스케일(ppu) 존재 | PASS |
| 9 | 스프라이트 프레임 통계 — `1925 프레임 / 14 시트` | PASS |
| 10 | 무기 3종 각각 필수 클립 10종 보유 | PASS |
| 11 | 적 6종 각각 필수 클립 6종 보유 | PASS |
| 12 | 보스 클립 10종 보유 | PASS |
| 13 | VFX 스프라이트 8종 보유 | PASS |
| 14 | 8방향 해석 + 미러링 동작 | PASS |
| 15 | 플레이어는 8방향 모두 실제 렌더(미러 없음) | PASS |
| 16 | 클립 재생: 루프/원샷 종료 처리 | PASS |
| 17 | 기본 저장 생성 | PASS |
| 18 | 저장 왕복(export/import) | PASS |
| 19 | 잘못된 JSON 안전 처리 | PASS |
| 20 | NaN/음수/범위이탈 값 정규화 | PASS |
| 21 | 존재하지 않는 업그레이드 ID 제거 | PASS |
| 22 | 저장 마이그레이션 (v1 -> 현재) | PASS |
| 23 | 설정 범위 클램프 | PASS |
| 24 | seeded random 재현성 | PASS |
| 25 | 방 생성: 첫 방은 항상 일반 전투 | PASS |
| 26 | 방 생성: 특수방 연속 등장 없음 | PASS |
| 27 | 방 생성: 총 7방 구성 | PASS |
| 28 | 적 스폰 위치: 벽 밖/플레이어 위 생성 없음 | PASS |
| 29 | 잔향 제안 중복 없음 | PASS |
| 30 | 잔향 최대 중첩 초과 불가 | PASS |
| 31 | 영구 업그레이드 상한 | PASS |
| 32 | 결정 부족 시 구매 거부 | PASS |
| 33 | 사망 적응 상한 (3단계) | PASS |
| 34 | 적응이 피해를 실제로 감소 | PASS |
| 35 | 첫 방 사망도 최소 보상 지급 | PASS |
| 36 | 생명 코어 최대 2개 제한 | PASS |
| 37 | 무기 데이터 3종 + 스프라이트 존재 | PASS |
| 38 | 적 데이터 6종 + 스프라이트 존재 | PASS |
| 39 | 엘리트 속성 3종 | PASS |
| 40 | 잔향 18종 이상 · ID 고유 · 설명 동작 | PASS |
| 41 | 영구 업그레이드 12종 이상 · ID 고유 | PASS |
| 42 | 보스 3단계 정의 | PASS |
| 43 | 행렬 수학 정확도 (invert/perspective/lookAt) | PASS |
| 44 | 월드/플레이어 생성 | PASS |
| 45 | 생명 코어 1개 소비 후 50% 체력 부활 | PASS |
| 46 | 무적 시간 중 피해 무시 | PASS |
| 47 | 생명 코어 중복 소비 없음 | PASS |
| 48 | 보스 단계 전환 중복 없음 · 승리 1회 처리 | PASS |
| 49 | 죽은 적은 추가 피해를 받지 않음 | PASS |
| 50 | UI 초기화 및 버튼 동작 | PASS |
| 51 | 모든 화면 빌더가 예외 없이 실행 | PASS |
| 52 | 스프라이트 시트 파일 실제 로딩 — `15개 모두 성공` | PASS |
| 53 | 시트 해상도가 매니페스트와 일치 | PASS |
| 54 | 시트 GPU 텍스처 업로드 — `14/14` | PASS |

## 3. 렌더 파이프라인 실측

실제 게임 코드로 한 프레임을 구성해 draw list 를 검사했습니다.

```
draw list 56 엔트리 → 18 배치 / 56 스프라이트
  floor                   1   (타일링 바닥)
  sheet_player_riftsword  1   (플레이어, cell 1, scale 1.43)
  sheet_props            33   (벽/기둥/소품)
  sheet_stalker           2   (적)
  sheet_vfx              19   (그림자/예고/파티클)
```

## 4. 항목별 결과

| 요구 항목 | 결과 |
|---|---|
| 스프라이트 셰이더 컴파일/링크 | 통과 (모킹 GL 기준) |
| 스프라이트 시트 로딩 | 15/15 성공, 해상도 전부 일치 |
| 8방향 + 미러링 | 통과 (플레이어 8실렌더 / 적 5+3미러) |
| 새 게임 → 사망 → 허브 | 통과 |
| 영구 성장 다음 런 반영 | 통과 (최대체력 100→112) |
| 생명 코어 부활 | 통과 (50% 부활·무적·중복소비 없음) |
| 보스 3단계 + 승리 1회 | 통과 |
| 사망 적응 | 통과 (투사체 피해 100→96) |
| 저장/복구/이어하기 | 통과 |
| 일시정지 중 시뮬레이션 정지 | 통과 |

## 5. 금지 패턴 스캔

`index.html` · `tests.html` · `scripts/` · `styles/` 전체에서 다음 **모두 0건**:
`fetch(` `XMLHttpRequest` `type="module"` `import` `export` `require(` `https://` `http://` `TODO` `FIXME`

- `'use strict';` : 21개 스크립트 전부
- 전역 네임스페이스: `window.EchoHeart` 하나뿐
- `package.json` / `node_modules` 없음

## 6. 콘솔 오류

스크립트 로드·헤드리스 구동·자체 테스트 전 구간에서 **확인된 오류 0건**.
`console.error` 를 무력화하는 코드는 없습니다.

## 7. 수정한 주요 오류

### 최우선 — 적이 예고만 하고 공격하지 않던 버그 (사용자 제보)
`Enemy.update()` 끝의 `setState(move ? 'move' : 'idle')` 가 **조건 없이 실행**되어,
같은 프레임에 `beginTell()` 이 설정한 `'tell'` 상태를 매번 덮어썼습니다. 결과:

- 적은 `cd<=0` 인 동안 **매 프레임 새 예고를 생성** → 화면이 예고로 도배됨
- `stateTime` 이 매 프레임 0으로 리셋 → 예고 지속시간에 영원히 도달 못 함 → **실제 공격 0회**
- 즉 사용자가 겪은 "예고 도배"와 "공격 안 함"은 **같은 하나의 버그**였습니다.

수정: 행동 상태(`tell`/`attack`/`recover`)일 때는 이동 상태로 덮어쓰지 않도록 가드 추가.
추가로 `beginTell()` 진입 시 쿨다운을 잠가 윈드업 중 재진입을 막았습니다.

검증(각 적 10초 시뮬레이션, 수정 전 → 후):

| 적 | 예고 | 공격 | 플레이어 피해 |
|---|---|---|---|
| 추적자 | 0 → 3 | 0 → 2 | 0 → 190 |
| 시간 사수 | 0 → 4 | 0 → 3 | 0 → 27 (투사체 발사) |
| 폭주 구체 | 0 → 1 | 0 → 자폭 | 0 → 20 |
| 방패병 | 0 → 3 | 0 → 3 | 0 → 39 |
| 잔향 소환사 | 0 → 4 | 0 → 3 | 0 → 540 |
| 궤도 감시자 | 0 → 3 | 0 → 3 | 0 → 24 (탄막 8발) |

회귀 테스트 3종 추가: 6종 전부 실제 공격 여부 / 예고가 매 프레임 재생성되지 않음 /
행동 상태가 이동 상태에 덮어써지지 않음.

### 그 외 이번 라운드
- **예고 과다 노출** — 동시 표시 상한 3개, 스폰 표시 원 제거, 공격 쿨다운 완화, 첫 공격 타이밍 분산
- **예고가 너무 밝아 캐릭터를 가림** — 해저드 스트라이프/맥동 제거, 최대 알파 1.00 → 0.34~0.59
- **때린 게 안 보임** — 명중 여부와 무관하게 휘두름 궤적 표시, 접촉점 스파크+바닥 섬광,
  피격 발광 3.4배 지속, 무기 궤적 알파 0.55→0.92, 판정 관대화(사거리 +0.25 / 각도 +0.18rad)
- **적이 벽 밖으로 / 지형물 통과** — 지형물 원형 충돌 추가, 분리 처리 후 경계 재클램프, 사망 중에도 클램프

### 이전 라운드
- `file://` WebGL 텍스처 업로드 실패로 전 표면이 흰 박스 → 텍스처 61장 data URI 내장
- 파트 피벗 이중 차감으로 캐릭터 붕괴 / 메시 91개 파트 내부향 / 방 즉시 자동완료 /
  스폰 타이머 잔존 / 보스 등장 중 시뮬레이션 정지

## 8. 검증하지 못한 항목

1. **실제 브라우저 렌더링 결과** — 화면 픽셀, 스프라이트 정렬·접지의 육안 확인, 색·발광의 실제 느낌.
2. **실제 GLSL 드라이버 컴파일** — 모킹 컨텍스트는 항상 성공을 반환합니다. 대신 정적 린트로
   6개 셰이더의 버전·중괄호/괄호 균형·출력 선언·선언되지 않은 uniform·렌더러가 설정하는
   30개 uniform 존재를 교차 검증했습니다(통과).
3. **프레임레이트/성능** — 실기 60fps 목표는 측정하지 못했습니다.
4. **실기 터치 조작** — 멀티터치·포인터 취소·회전 후 좌표 일치는 코드 경로만 검토.
5. **검수 해상도별 레이아웃** (1280×720, 844×390, 932×430, 390×844, 430×932, 1920×1080).
6. **WebGL context loss 실제 복구**, **오디오 실제 출력**, **localStorage 차단 환경 동작**.

### 사용자가 직접 확인하는 방법

1. `index.html` 더블클릭 → 로딩 → 타이틀 → 새 게임 → 허브 → 런
2. `tests.html` 더블클릭 → 자체 테스트 54개 결과 확인 (실제 GL·실제 시트 로딩 기준)
3. F12 콘솔에서 오류 유무 확인

## 최종 검수 (프리렌더 스프라이트 적용 후)

| 항목 | 결과 |
|---|---|
| 스크립트 문법 검사 | 35/35 통과 |
| 셀프테스트 (`tools/run_selftest.js`) | 66/66 통과 |
| 통합 테스트 (`tools/headless_test.js`) | 56/56 통과 |
| 게임 루프 런타임 오류 | 0 |
| 콘솔 오류 (전체 흐름 1회 주파) | 0 |

### 스프라이트 파이프라인 실증
- 시트 11장 전부 텍스처 업로드 / 매니페스트 등록 / 데이터URI 임베드 확인 (누락 0)
- 무기 3종 각각 전용 시트로 전환 확인
- 적 6종 + 보스 시트 매핑 누락 0
- 상태별 행 선택: 대기 0 / 이동 8 / 공격 16 / 사망 24 (기대값 일치)
- 8방향 선택: 후방 = 행 12 (기대값 일치)
- 단발 클립(공격·사망)이 마지막 프레임에서 정지하는지 확인
- 생명 코어 부활 → 2차 사망 시 런 종료까지 정상
- 설정 `스프라이트 그래픽`=0 → 실시간 3D 폴백 동작 확인

### 알려진 사항
- `assets/sprites/` 의 구버전 시트(8.8MB)는 어디에서도 로드되지 않습니다.
  `scripts/sprite-data.js` / `sprite-renderer.js` / `sprite-anim.js` 도 index.html 에 포함돼 있지 않습니다.
  (실행 환경에서 파일 삭제 권한이 없어 남겨 두었습니다. 지워도 게임은 정상 동작합니다.)
- `assets/sprites_v2/*.partial.png` 는 베이크 중간 체크포인트 파일이며 런타임에 쓰이지 않습니다.

### 스프라이트 접지·스케일 보정 (최종)
빌보드는 발밑 기준으로 그려지는데 베이크된 셀은 캐릭터가 중앙에 놓여 있어,
그대로 두면 캐릭터가 자기 키의 15~31%만큼 공중에 떠 보이는 문제가 있었습니다.
시트별로 셀 내부 점유 구간을 실측해 `footPad`(접지 보정)와 `scale`(모델 실제 높이 기준)을
메타에 기록하고 렌더에 반영했습니다.

| 검증 | 결과 |
|---|---|
| 플레이어 발 위치 | y=0.02 (바닥, z-fighting 오프셋만) |
| 플레이어 보이는 높이 | 1.76 = 모델 높이와 일치 |
| 적 stalker 쿼드 | 1.75 (메타 scale 1.747) |
| 기둥(scale=1) 보이는 높이 | 2.20 = 메시 높이와 일치, 발 y=0.000 |

통합 테스트를 12회 반복 실행해 전부 56/56 통과를 확인했습니다.
(중간에 1회 55/56 이 관측돼 하네스의 임의 반복 상한을 웨이브 8→14, 방 12→20 으로 상향했습니다.
게임 로직이 아닌 테스트 스캐폴딩의 여유값입니다.)

## 무기 상성 도입 + 무오류 강화 (최종)

| 스위트 | 결과 |
|---|---|
| 스크립트 문법 검사 | 35/35 |
| 셀프테스트 `tools/run_selftest.js` | 71/71 (상성 무결성 5건 신규) |
| 통합 테스트 `tools/headless_test.js` | 63/63 (지연 타이머 4건 + 아레나 경계 3건 신규) |
| 퍼즈·소크 `tools/fuzz_test.js` | 14/14, 예외 0 |

### 퍼즈 테스트가 검증하는 것
- 랜덤 입력 폭풍 + 적대적 dt(0 / 음수 / 5초 / NaN) 수천 프레임 → 예외 0
- NaN·Infinity 강제 주입 → 다음 프레임에 자동 복구
- 무기3 × 적6 × 엘리트3 = 54조합 전투 → 예외 0
- 보스 3페이즈 × 무기3 → 저항값 전부 유효
- 손상된 세이브 8종(잘린 JSON, NaN, 범위 초과, 없는 무기) → 전부 복구
- 3000프레임 소크 → 오브젝트 풀 상한 준수(누수 없음)

### 이번에 발견해 고친 실제 버그
1. **`slowmoT` 미초기화** — 생성자에 없어 산술 시 NaN 발생. 초기화 + 감산 바닥 고정.
2. **NaN 타임스탬프가 시계를 영구 오염** — `this.last`가 한 번 NaN이 되면 이후 모든 dt가 0이 되어 게임이 멈춤. 루프에서 방어.
3. **상성 이중 적용 위험** — `computeHit`과 `damageEnemy` 양쪽에 걸면 경로에 따라 2번 곱해짐. `damageEnemy` 한 곳으로 단일화.
4. **밸런스 측정이 비결정적** — 엔진이 `Date.now()`로 시드를 잡아 같은 측정이 실행마다 달랐음. 하네스에서 시드 고정. (이 때문에 초기 튜닝값이 잘못 나왔었음)
5. **측정 코드 이중화로 인한 드리프트** — 튜너와 시뮬레이터가 각자 측정해 서로 다른 결론. 튜너가 시뮬레이터 결과를 쓰도록 단일화.

### 상시 회귀 방어
저항 테이블의 세 열 합계가 동일한지, 무기마다 강함2/약함2 대칭인지를 셀프테스트가 매번 검사합니다. 수치를 잘못 건드리면 즉시 실패로 잡힙니다.

### 프레임 기반 지연으로 전환 (setTimeout 제거)

방 클리어 보상(0.7초), 승리(2.6초), 사망(1.9초) 연출 지연이 `setTimeout` 이었습니다. 벽시계 타이머라서:

- **일시정지 중에도 흘러갑니다.** 방을 클리어한 직후 일시정지하면 보상 화면이 일시정지 메뉴 위로 튀어나옵니다.
- 탭을 백그라운드로 두면 프레임은 멈추는데 타이머만 진행됩니다.
- 테스트가 벽시계와 경합해 CPU 부하 시 간헐적으로 실패했습니다.

`Game.after(초, 콜백)` + `tickTimers(dt)` 로 교체했습니다. 일시정지·모달 검사 **뒤에** 틱하므로 메뉴가 열려 있으면 지연도 함께 멈추고, 새 런 시작 시 이전 타이머는 폐기됩니다. 콜백 예외는 잡아서 루프를 죽이지 않습니다.

**이 전환이 드러낸 실제 널 참조 버그:** `finishRun()` 이 프레임 *안에서* 실행되면서 `this.run` 을 null 로 만드는데, 같은 프레임의 이후 코드가 `this.run.elapsed` 를 그대로 참조했습니다. setTimeout 시절에는 프레임 사이에 실행돼 드러나지 않던 문제입니다. 틱 직후 모드/런 재검사를 추가해 해결했습니다.

회귀 방지로 통합 테스트에 4건을 추가했습니다: 일시정지 중 미발동 / 재개 후 발동 / 새 런 시 이전 타이머 폐기 / 콜백 예외 내성.

통합 테스트를 9회 연속 실행해 전부 60/60 을 확인했습니다.

## 원형 아레나 경계 버그 (사용자 제보)

> "몹이랑 싸우다가 대시로 동그란 벽 밖으로도 나갔다 들어오던데?"

**원인.** 벽은 `rooms.js` 에서 **원형**으로 배치됩니다 (`cos/sin × (half+0.6)` = 반경 9.6). 그런데 경계 클램프 `clampToRoom()` 은 x·z 를 각각 독립적으로 자르는 **사각형** 클램프였습니다.

- 정면 방향: 한계 9.0 → 벽 안쪽, 정상
- **대각선 방향: 한계 9×√2 ≈ 12.7 → 벽(9.6) 보다 3.1 바깥**

평소엔 이동 속도가 느려 잘 드러나지 않다가, 대시로 대각선을 향하면 순간이동하듯 벽을 통과했습니다.

**수정.**

1. `clampToRoom()` 을 원형 경계로 교체. 넘어가면 중심 방향으로 되돌리고, 속도·넉백의 바깥 방향 성분을 제거해 벽을 긁지 않고 미끄러지게 했습니다.
2. `confine()` 이 "벽에 닿았는지" 를 반환하도록 하고, **대시는 벽에 닿는 순간 종료**됩니다(임팩트 파티클 포함). 벽을 따라 질질 끌리지 않습니다.
3. 무결성 스윕의 범위 검사도 사각형에서 원형으로 통일했습니다.
4. 벽이 원형인데 바닥은 사각 판이라 벽 바깥으로 바닥이 삐져나와 있었습니다. PBR 셰이더에 `uClipR` 을 추가해 바닥을 원판으로 잘라냅니다.

**검증** (통합 테스트 상시 포함)

| 항목 | 결과 |
|---|---|
| 8방향 × 4회 대시 (대각선 포함) | 최대 반경 8.58 / 한계 8.60, 위반 0프레임 |
| 극단적 넉백 (200, 150) 30프레임 | 반경 8.58 로 유지 |
| 반경 30 밖에 스폰된 적 8기 | 20프레임 내 8.50 으로 수렴 |
| 벽 접촉 시 대시 종료 | 정상 |

## 간헐 실패의 진짜 원인 (해결)

통합 테스트가 8~10회에 1회꼴로 실패했습니다. 세 가지 벽시계 의존이 원인이었고 전부 제거했습니다.

1. **연출 지연이 `setTimeout`** — 보상/승리/사망 지연이 실제 시간으로 흘러 테스트와 경합. → 프레임 기반 타이머로 교체 (일시정지 정확성도 함께 해결).
2. **하네스의 고정 대기** — 사망·승리 후 `setTimeout(2200)` 으로 기다림. → 프레임 진행으로 교체.
3. **에셋 로딩 고정 대기 60ms** — 목 이미지 100여 개가 각자 `setTimeout(0)` 으로 로드를 알리는데, 부하가 걸리면 60ms 안에 다 소진되지 않아 스프라이트·텍스처 관련 검사가 무더기로 실패. → `G.started` 폴링으로 교체.

3번이 가장 큰 원인이었습니다. 이제 CPU 를 4개 프로세스로 점유한 상태에서도 63/63 을 유지합니다.

**남은 벽시계 의존: 없음.** 게임 로직과 테스트 모두 프레임 기반입니다.

## 성능 (렉) 개선

"렉이 너무 심함" 제보. 추측 대신 측정부터 했습니다.

**측정 결과.** 평상시 91 드로우콜, 그런데 **적 14기 + 궁극기 순간 399 드로우콜**. 원인은 파티클이었습니다. 최대 320개가 살아있고 **개당 별도 드로우콜**(행렬 생성 + 유니폼 업로드 + draw)이라, 하필 게임이 가장 바쁜 순간에 부하가 4배로 튀었습니다.

**1. 파티클 배치 렌더링 (가장 큰 효과)**

전용 셰이더(`partVS/partFS`)와 동적 버퍼를 추가해 모든 파티클을 카메라를 향한 쿼드로 모아 **한 번의 draw** 로 그립니다. 원형 감쇠는 프래그먼트에서 계산해 텍스처도 필요 없습니다.

| | 이전 | 이후 |
|---|---|---|
| 최악 드로우콜 (적14+궁극) | 399 | **91** |

**2. 적응형 해상도 governor**

기기 성능을 모르니 고정 품질 대신 프레임 시간을 재서 자동 조절합니다. EMA 로 측정해 45fps 아래면 렌더 해상도를 10%씩 낮추고(하한 0.55), 58fps 위로 올라오면 5%씩 되돌립니다(상한 1.0). 조정 후 1초 쿨다운으로 진동을 막습니다.

- 30fps 기기 흉내 → resScale 0.55 로 하향, 픽셀 수 64% 감소
- 83fps 로 회복 → 1.00 복귀

가장 무거운 표면은 화면을 가득 채우는 바닥입니다. PBR 셰이더가 픽셀마다 텍스처 4장 + 미분 기반 접선 프레임을 계산하므로, 해상도를 줄이는 것이 가장 직접적인 처방입니다.

**3. 이방성 필터 8 → 4**

이 카메라 각도에서는 육안 차이가 없는데 전체화면 바닥의 픽셀당 텍스처 페치가 대략 절반이 됩니다.

**4. 파티클 수를 governor 에 연동**

기기가 버거우면 이펙트 개수도 함께 줄입니다 (최대 65% 감소).

**5. FPS 표시**

설정 → **FPS 표시 (성능 확인용)** 으로 `현재fps / 해상도배율 / 드로우콜` 을 화면 상단에 띄웁니다.

**회귀 방지.** 통합 테스트에 드로우콜 예산 검사(최악 150 이하)를 상시 포함했습니다. 현재 최악치 91.

## 성능 2차: 기본 설정에서 렉 없게

"저사양 모드 안 해도 기본으로 렉 없어야 한다" — 맞는 지적입니다. 적응형 해상도는 증상 완화였고, 실제 GPU 비용을 네 군데에서 찾아 없앴습니다.

**1. 픽셀 예산 (가장 큰 원인)**

디스플레이 해상도에 따라 프래그먼트 비용이 그대로 폭증하고 있었습니다.

| 화면 | 이전 | 이후 |
|---|---|---|
| 폰 390×844 @dpr3 | 1.32MP | 1.32MP (그대로, 선명함 유지) |
| 노트북 1280×720 | 0.92MP | 0.92MP (그대로) |
| FHD @dpr2 | 8.29MP | **2.30MP** (3.6배 감소) |
| QHD @dpr2 | 14.75MP | **2.30MP** (6.4배 감소) |

기기 종류를 추측하지 않고 총 픽셀 수에 상한을 둡니다. 작은 화면은 원래 배율을 유지해 선명하고, 큰 고해상도 화면만 제한됩니다. 비용을 결정하는 건 기기 이름이 아니라 픽셀 수이기 때문입니다.

**2. 하늘 오버드로 제거**

하늘이 **깊이 테스트 없이 맨 먼저** 전체 화면에 그려지고, 바닥이 그 위를 덮었습니다. 화면 하나 분량의 셰이딩이 통째로 버려지던 셈입니다. 이제 불투명 물체 뒤에 깊이 테스트를 켜고 그려서, 실제로 비어 있는 배경 픽셀만 계산합니다.

**3. 바닥 접선 프레임 상수화**

바닥은 화면을 가득 채우는데 픽셀마다 `cotangent()` 로 **미분 4회 + 외적 2회**를 돌려 접선 프레임을 구하고 있었습니다. 바닥은 Y-up 평면이라 이 값이 상수입니다. `uFlatTBN` 분기(유니폼이라 분기 발산 없음)로 전부 생략합니다.

**4. MSAA 비활성 + 이방성 8→4**

MSAA 는 전체 화면 대역폭을 배로 늘립니다. dpr 2 에서 얻는 선명함 대비 비용이 크지 않아 껐습니다.

**적응형 해상도는 이제 안전망 역할만** 합니다. 개입 문턱을 45fps → **40fps** 로 낮추고 하한도 0.55 → 0.6 으로 올려서, 기본 화질을 최대한 유지합니다.

**회귀 방지.** 통합 테스트에 픽셀 예산(2.4MP 이하)과 MSAA 비활성 검사를 상시 포함했습니다.

## 그래픽 임팩트 (접지 그림자 + 블룸)

성능을 잡은 예산 안에서 체감이 가장 큰 두 가지를 넣었습니다.

**1. 접지 그림자**

캐릭터가 바닥에 떠 보이던 문제의 근본 원인이었습니다. 액터·구조물마다 부드러운 그림자를 깔되, 높이에 따라 커지고 흐려지게 해서 점프·부유 상태도 자연스럽습니다. 파티클과 같은 배치 버퍼를 재사용하고 지면 정렬 쿼드로 그려서 **전부 합쳐 드로우콜 1개**, 곱셈 블렌딩으로 실제로 어둡게 깔립니다.

**2. 블룸 (발광 번짐)**

네온 게임인데 빛이 번지지 않아 페인트처럼 보였습니다. 오프스크린 타깃 → 밝은 영역 추출(임계 0.48, soft knee) → **1/4 해상도** 분리형 가우시안 2패스 → 합성. 비싼 블러를 1/4 해상도에서만 하므로 총 4패스로 끝납니다. 합성 단계에서 은은한 비네트도 함께 적용해 시선이 아레나 중앙으로 모입니다.

| 항목 | 값 |
|---|---|
| 최악 드로우콜 (적14+궁극+그림자+블룸) | **97** / 예산 150 |
| 블룸 추가 비용 | 4패스 (1/4 해상도) |
| 접지 그림자 추가 비용 | 1드로우콜 |

**안전장치**

- 저사양 모드에서는 블룸이 자동으로 꺼집니다.
- 설정 → **발광 번짐 (블룸)** 으로 직접 끌 수 있습니다.
- 프레임버퍼 생성에 실패하는 환경에서는 블룸 없이 정상 렌더로 폴백합니다 (검증됨).
- 컨텍스트 손실 후 재초기화 시 오프스크린 타깃을 해제하고 다시 만듭니다. 리사이즈 시에도 재생성합니다.

**부수적으로 고친 것.** `showFps` 와 `bloom` 이 `BOOL_SETTINGS` 화이트리스트에 없어서 토글해도 저장되지 않았습니다. 추가했습니다.

**회귀 방지.** 통합 테스트에 5건 추가: 그림자 생성 수, 블룸 4패스, 1/4 해상도, 저사양 자동 비활성, 컨텍스트 재초기화 후 타깃 재생성.

## 탑 등반 구조 (입체감)

"그냥 동그란 원에서 싸우는 게 아닌, 탑을 올라가는 느낌" — 아레나를 탑의 층계참으로 재구성했습니다.

**층 구성.** 방 진행을 층수로 매핑하고 4개 구역으로 묶었습니다. 테마는 `EH.TOWER` 한 곳에서 파생되므로 HUD 표시와 실제 화면이 어긋날 수 없습니다.

| 방 | 층 | 구역 | 테마 | 아래 링 수 |
|---|---|---|---|---|
| 0-1 | 9·18층 | 하층부 (기관층) | engine | 7 |
| 2-3 | 27·36층 | 중층부 (기록층) | archive | 9 |
| 4-5 | 45·54층 | 상층부 (용광로층) | forge | 11 |
| 6 | 63층 | 첨탑 (심장부) | heart | 13 |

**수직 배경.** 발판이 허공에 뜬 원반으로 읽히도록 세 가지를 그립니다.

1. **발판 두께(림)** — 3겹 링으로 원반의 단면을 표현. 이전엔 바닥에 그린 원처럼 보였습니다.
2. **아래로 내려가는 샤프트** — 동심 링이 안개 속으로 멀어집니다. 위로 올라갈수록 링이 많아지고 간격이 넓어져 "더 높이 왔다"가 보입니다.
3. **수직 지지대** — 링들을 잇는 구조물. 위층으로 갈수록 개수가 줄어 탑이 가늘어지는 느낌을 냅니다.

전부 안개로 페이드되는 언릿 지오메트리라 게임플레이를 가리지 않고, 총 **20 드로우콜**입니다.

**상승 연출.** 방에 진입하면 카메라가 5.5유닛 아래에서 시작해 1초에 걸쳐 올라오고, 발판 아래에서 위로 흐르는 입자가 뿜어집니다. 동시에 `63층 / 첨탑 · 심장부` 배너가 뜹니다.

**층수 HUD.** 우측에 현재 층수·구역과 세로 등반 진행 게이지를 표시합니다. 구역별로 색이 바뀝니다 (시안 → 청색 → 주황 → 진홍).

**성능.** 첨탑(링 13개) + 적 14기 + 궁극기 + 그림자 + 블룸을 모두 겹친 최악 상황에서 **112 드로우콜** / 예산 150.

**회귀 방지.** 통합 테스트 6건 추가: 층수 증가, 구역-테마 일치, 4구역 전부 사용, 첨탑 도달, 배경 렌더, 상승 연출 복귀.

## 허브 바닥 실종 (탑 배경 도입 시 발생한 회귀)

> "로비가 왜 이따구냐.. 바닥이 없네"

제가 넣은 탑 배경이 만든 회귀입니다. 두 가지가 겹쳤습니다.

**1. 셰이더 프로그램 상태 누수 (바닥 실종의 원인)**

`drawMesh()` 는 `begin()` 이 PBR 프로그램을 활성화해 뒀다고 **가정**하고 자기 프로그램을 바인딩하지 않았습니다. 그런데 새로 넣은 `drawTowerBackdrop()` 이 unlit 프로그램으로 전환한 뒤 되돌리지 않아서, 뒤이어 그려지는 바닥이 PBR 유니폼을 unlit 프로그램에 쏟아붓고 화면에서 사라졌습니다.

같은 함정이 스프라이트·파티클·블룸 패스에도 잠재해 있었습니다. 근본 수정:

- `drawMesh()` 가 **자기 프로그램을 직접 바인딩**합니다 (`_curProg` 캐시로 중복 호출 없음).
- 프로그램을 전환하는 **모든** 지점(unlit·sprites·sky·particles·bright·blur·composite)이 `_curProg` 를 갱신합니다. 한 곳(`endParticles`)이 누락돼 벽·크리스탈이 파티클 셰이더로 그려지고 있었는데, 추적을 붙이니 바로 잡혔습니다.

**2. 거대한 대각선 빔**

지지대를 `bolt` 프림에 오일러 회전을 걸어 세우려 했는데 회전 순서가 의도와 달라 화면을 가로지르는 대각선 하나가 됐습니다. 세로 쿼드(`card`)로 교체해 확실히 수직이 되게 했습니다.

**회귀 방지.** 통합 테스트 3건 추가:

| 검사 | 내용 |
|---|---|
| 허브 메시가 모두 PBR 로 렌더 | 잘못된 프로그램으로 그려지는 메시 0 |
| 전투 중에도 유지 | 적 8기 + 공격 40프레임 후에도 0 |
| 허브에 바닥이 실제로 그려짐 | `floorTile` 드로우 호출 확인 |

전 층(허브 → 7개 방) 주파 시에도 잘못된 프로그램 바인딩 0건을 확인했습니다.

### 덤: 근접 공격 테스트 간헐 실패 (8회 중 1회) 원인 규명

허브 버그를 고치며 돌린 반복 실행에서 `melee attack damages an enemy` 가 가끔 실패했습니다. 원인 둘 다 하네스 쪽이었습니다.

1. **런 시드가 고정돼 있지 않았습니다.** 엔진이 `Date.now()` 로 시드를 잡아 방 구성이 매 실행 달라졌고, 그래서 `enemies[0]` 에 어떤 적이 오는지가 매번 바뀌었습니다.
2. **사수·감시자가 걸리면 정당하게 도망칩니다.** 이 둘은 거리를 벌리는 AI라, 제자리에 선 플레이어의 근접 공격을 40프레임 동안 피할 수 있습니다. 근접이 고장난 게 아니라 적 AI가 제대로 동작한 것입니다.

수정: 하네스에 시드를 고정하고, 근접 테스트는 대상을 제자리에 고정합니다(이 테스트의 관심사는 "근접이 맞는가"이지 "도망가는 적을 따라잡는가"가 아니므로).

고친 뒤 6회 연속 81/81. 남은 실행 간 차이는 보스 패턴 선택 같은 정보성 문구뿐이며 판정에는 영향이 없습니다.

## 스킬이 안 나가던 문제 (입력 우선순위)

> "마우스 우클릭하니까 스킬 안 나가고 공격 나가던데?"

우클릭은 증상이었고, 실제로는 **모든 입력 방식에서 공격을 누르고 있으면 스킬·궁극기가 아예 안 나가는** 상태였습니다. 측정 결과 **12회 시도 중 0회 발동**.

**원인 1 — 처리 순서 (핵심).** `Player.update()` 가 공격을 먼저 처리했습니다. `startAttack()` 이 `atkPhase` 를 세우면 `canAct()` 가 false 가 되고, 그 뒤에 오는 `input.take('skill')` 이 입력을 **소비만 하고 버렸습니다.** 공격 홀드는 정상적인 플레이 방식이므로 사실상 스킬을 못 쓰는 상태였습니다.

**원인 2 — 우클릭.** `self.mouse.down = true` 가 버튼 종류와 무관하게 실행돼, 우클릭이 `attackHeld` 를 켜고 공격을 발동시켰습니다.

**수정**

1. 스킬·궁극기를 **기본 공격보다 먼저** 처리합니다.
2. **입력 버퍼링(0.32초)** — 지금 쓸 수 없는 순간에 눌러도 기억했다가 가능해지면 발동합니다.
3. **후딜 캔슬** — 스킬·궁극기는 공격의 회수 동작(recover)을 끊고 나갑니다. 선딜·타격 프레임은 그대로 유지되므로 공격이 헛돌지 않습니다.
4. 좌클릭만 `mouse.down` 을 켭니다.

| | 이전 | 이후 |
|---|---|---|
| 공격 홀드 중 스킬 (12회) | 0회 | **12회** |
| 공격 홀드 중 궁극기 (12회) | 0회 | **12회** |
| 발동 지연 | — | 평균 57ms (평상시 0ms) |

**회귀 방지.** 통합 테스트 3건 추가: 공격 홀드 중 스킬 12/12, 궁극기 12/12, 우클릭이 공격을 켜지 않음.

## 끊김의 정체 — FPS 가 아니라 연출 과다

> "와씨 이거 너무 끊기는데?"

프레임 문제가 아니었습니다. 측정해보니 **연출이 게임을 계속 멈추고 있었습니다.**

| 지표 (적 8기 밀착, 공격 홀드 10초) | 이전 | 이후 |
|---|---|---|
| 히트스톱 활성 프레임 | **19%** | **6%** |
| 최장 연속 정지 | 100ms | 50ms |
| 정지 시 시간배율 | 0.12 (사실상 완전정지) | 0.30 |
| 체감 화면흔들림 | 74% | 9% |
| 흔들림 최대 진폭 | 0.60 | 0.115 |

**원인.** 타격 하나하나가 50~110ms 씩 화면을 멈췄고, 연타하면 그 정지가 줄줄이 이어졌습니다. 카메라도 감쇠(반감기 0.11초)보다 타격이 빨라서 사실상 항상 흔들리고 있었습니다. 제가 "타격감" 작업에서 과하게 넣은 값들입니다.

**수정**

1. **히트스톱 등급화** — 기본 타격 26ms, 강타격·스킬 60ms (이전 일괄 50~110ms).
2. **히트스톱 쿨다운 0.2초** — 연속 타격이 정지를 이어붙이지 못하게 막습니다. 쿨다운 중에도 약한 카메라 킥은 남겨 타격감이 죽지 않게 했습니다.
3. **정지 강도 완화** — 시간배율 0.12 → 0.30. 여전히 임팩트로 읽히되 게임은 계속 흐릅니다.
4. **흔들림 축소** — 진폭 상한 0.6 → 0.15, 감쇠 반감기 0.11초 → 0.043초.

**작업 중 스스로 만든 버그.** `this.shakeMax` 값을 바꾸며 한 줄에 여러 문장이 있는 곳에 주석을 붙였다가 뒤따르던 `this.shakeT = 0;` 이 주석 처리됐습니다. `shakeT` 가 undefined → `+= dt` 에서 NaN → 카메라 행렬 전체가 NaN. 기존 테스트(`no NaN in camera matrices`)가 즉시 잡아냈습니다.

**밸런스 재튜닝.** 히트스톱이 줄면서 실제 전투 시간이 바뀌어 무기 편차가 3.4% → 10.7% 로 벌어졌습니다. 자동 튜너를 돌려 **1.6%** 로 복구했고, 상성 6개는 그대로 유지됩니다.

**회귀 방지.** 통합 테스트 4건 추가: 히트스톱 12% 이하, 연속 정지 4프레임 이하, 체감 흔들림 25% 이하, 흔들림 진폭 상한.
